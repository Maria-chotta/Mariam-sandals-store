{
  "name": "mariam-sandal-store",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p build && cp -R frontend/*.html frontend/*.css frontend/*.js frontend/images .nojekyll build/",
    "prestart": "npm run build",
    "postinstall": "npm run build",
    "start": "node backend/server.js"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "mongoose": "^9.6.2"
  }
}
